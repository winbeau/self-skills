interface:
  display_name: "Article to HTML"
  short_description: "Render safe static articles with selectable styles"
  default_prompt: "Use $article-to-html to render this document as one safe self-contained HTML file. Use the xju-notion default unless I explicitly request paper-proposal, paper, or proposal."
